###corbot defaut params###
name: test_4
parameters:
      # defaut action is create but choice are multiple ["show", "create", "udpate", "add", "start", "restart", "stop", "export", "report", "delete"]
      action   : create
      #query with or without \"
      query : COP21 OR COP 21
      #a file for multiple urls
      file : 
      #An API KEY
      key: 
      # a simple url
      url : 
      #nb of bing results defaut is 1000
      nb : 10
      #Lang Not Implemented Yet
      #lang: fr
      #mail for report
      user: 4barbes@gmail.com
      #depth of crawl defaut is set to 10
      depth: 10
      #debug True or False defaut to False
      debug: 
      repeat: day
